<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cmpundhir.cm.allwidgetsdemo">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".layouts_demos.ScrollVIewActivity"></activity>
        <activity android:name=".layouts_demos.LinearLayoutDemoActivity" />
        <activity android:name=".LayoutsActivity" />
        <activity android:name=".WebViewActivity" />
        <activity android:name=".PermissionActivity" />
        <activity android:name=".DialogActivity" />
        <activity android:name=".intent_pckg.Next1Activity" />
        <activity android:name=".IntentActivity" />
        <activity android:name=".FormActivity" />
        <activity android:name=".ButtonActivity" />
        <activity android:name=".ToastActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>